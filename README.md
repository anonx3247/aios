# AIOS

A monorepo containing a desktop application with a backend service.

## Structure

- `apps/desktop` - Tauri desktop application (React + Rust)
- `apps/backend` - Backend service (TypeScript)

## Setup

```bash
pnpm install
```
