{
  "$schema": "https://tauri.app/schema/capabilities/default.json",
  "identifier": "default",
  "description": "Default permissions for AIOS",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
